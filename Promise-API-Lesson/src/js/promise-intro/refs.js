"use strict";

export const refs = {
  promiseApiOutput: document.getElementById("output"),
  beforePromise: document.getElementById("before_promise_output"),
  afterPromise: document.getElementById("after_promise_output"),
};
